/**
 * aiService.js — iaio Test v2
 *
 * (Offline Mode) All AI interactions are disabled.
 * Expects user to provide their own API key in future versions.
 */

async function analyzeBug() {
  return "Conecte su propia API Key de IA en Configuración para usar esta funcionalidad (Próximamente)"
}

module.exports = { analyzeBug }
